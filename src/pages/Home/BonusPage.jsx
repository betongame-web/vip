import BaseLayout from '@/components/layouts/BaseLayout';

export default function Bonus() {
  return (
    <BaseLayout>
      <div className="card-surface p-8">
        <h1 className="mb-3 text-3xl font-bold">Bonus</h1>
        <p className="max-w-3xl text-sm text-gray-400">Bonus rules and visuals were not fully represented in the API bundle, so this is a clean scaffold.</p>
      </div>
    </BaseLayout>
  );
}
